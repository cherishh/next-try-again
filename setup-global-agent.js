// setup-global-agent.js
require('global-agent/bootstrap');
console.log('Global agent bootstrapped via preloaded script.');
console.log('Ensure GLOBAL_AGENT_SOCKS_PROXY (e.g., to socks5h://127.0.0.1:1086) is set in your environment.');
